// import { messagesCollection } from '@/utils/firebase';
// import * as Sentry from '@sentry/vue';

/**
 * Vuex module for messages
 * @module messages
 * @requires firebase
 * @requires Sentry
 */
export default {
  /**
   * @name State
   * @type {object}
   */
  state: {},
  /**
   * @name Mutations
   * @type {object}
   */
  mutations: {},
  /**
   * @name Actions
   * @type {object}
   */
  actions: {
      setMessages() {
          // conversation id
          // artist id, client id, project id
          // messages : { date, content, sentBy(name)}
      }
  },
  /**
   * @name Getters
   * @type {object}
   */
  getters: {},
};
